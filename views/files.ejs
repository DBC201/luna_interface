<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Luna</title>
</head>
<body>
<% if (file_names === undefined || file_names.length == 0) {%>
    <h>No data</h>
<% } else { %>
    <% for (let i=0; i<file_names.length; i++) { %>
        <a href="/graph/<%= file_names[i]%>"><%= file_names[i]%></a><br>
        <br>
    <% } %>
<%}%>
</body>
</html>